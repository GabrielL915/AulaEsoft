export enum ErrorMessages {
    INVALID_NUMBER_RANGE = "quantidade de index é maior que o tamanho do array",
    INVALID_NUMBER = "quantidade de index é menor que 1",
    INVALID_ARRAY = "array vazio",
}